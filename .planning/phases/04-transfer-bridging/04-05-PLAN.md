---
phase: 04-transfer-bridging
plan: 05
type: execute
wave: 2
depends_on: ["04-04"]
files_modified: []
autonomous: false
gap_closure: true

must_haves:
  truths:
    - "The full codebase compiles and all warm transfer configuration is in place"
    - "No references to Twilio Conference infrastructure remain"
    - "The warm transfer flow is ready for live testing"
  artifacts: []
  key_links: []
---

<objective>
Verify the warm transfer architectural pivot is complete and the codebase is clean. Human verifies the configuration is correct before live testing.

Purpose: The architectural pivot from Twilio Conference to Vapi warm transfer touches multiple files and deletes infrastructure. This checkpoint ensures nothing was missed before attempting a live call.

Output: Confirmed clean codebase ready for live warm transfer testing.
</objective>

<execution_context>
@/Users/brad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-transfer-bridging/04-04-SUMMARY.md
@src/app/api/call/start/route.ts
@src/app/api/webhooks/vapi/route.ts
@src/lib/stall-prompt.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final build verification and dead code scan</name>
  <files></files>
  <action>
Run a comprehensive verification of the codebase after the warm transfer pivot:

1. `npm run build` -- must pass with zero errors and zero warnings related to missing modules.

2. Scan for any remaining references to deleted modules:
   - `grep -r "transfer-orchestrator" src/` -- should return nothing
   - `grep -r "twilio-client" src/` -- should return nothing (twilio.ts for OTP is fine)
   - `grep -r "conference-status" src/` -- should return nothing
   - `grep -r "conference-bridge" src/` -- should return nothing
   - `grep -r "TWILIO_CONFERENCE_BRIDGE_NUMBER" src/` -- should return nothing

3. Verify the deleted files no longer exist:
   - `ls src/lib/transfer-orchestrator.ts` -- should fail
   - `ls src/lib/twilio-client.ts` -- should fail
   - `ls src/app/api/webhooks/twilio/` -- should fail (directory removed)

4. Verify warm transfer configuration is in place:
   - `grep "warm-transfer-experimental" src/app/api/call/start/route.ts` -- should match
   - `grep "transferCall" src/app/api/call/start/route.ts` -- should match
   - `grep "transfer-update" src/app/api/call/start/route.ts` -- should match in serverMessages
   - `grep "assistant-forwarded-call" src/app/api/webhooks/vapi/route.ts` -- should match
   - `grep "transferCall" src/lib/stall-prompt.ts` -- should match

5. Verify the env var `TWILIO_CONFERENCE_BRIDGE_NUMBER` can be removed from `.env.local` if it exists there (informational -- do not modify .env files).

If any check fails, fix the issue before proceeding to the checkpoint.
  </action>
  <verify>
    All 5 verification groups pass. `npm run build` succeeds. No dead references found.
  </verify>
  <done>
    - Build passes
    - No dead references to Twilio Conference code
    - Warm transfer configuration confirmed in all three files
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete architectural pivot from Twilio Conference to Vapi warm transfer:

    **Deleted (Twilio Conference infrastructure):**
    - src/lib/transfer-orchestrator.ts (292 lines)
    - src/lib/twilio-client.ts (6 lines)
    - src/app/api/webhooks/twilio/conference-status/route.ts (64 lines)
    - src/app/api/webhooks/twilio/conference-bridge/route.ts (61 lines)

    **Modified:**
    - src/app/api/call/start/route.ts -- Added transferCall tool with warm-transfer-experimental mode, transfer assistant config, fallback plan, user phone as destination, "transfer-update" in serverMessages
    - src/app/api/webhooks/vapi/route.ts -- Removed orchestrateTransfer import/call, removed transfer-managed status guards, added warm transfer detection in end-of-call-report (assistant-forwarded-call -> connected, warm-transfer-assistant-cancelled -> failed), human_detected now sets transferring status directly
    - src/lib/stall-prompt.ts -- Updated to instruct AI to call transferCall immediately after introducing itself

    **Kept unchanged:**
    - src/app/call/[id]/call-status-client.tsx -- UI changes from 04-03 are still valid (transferring/connected states, failure reasons)
    - src/lib/twilio.ts -- OTP Verify (separate from deleted twilio-client.ts)
  </what-built>
  <how-to-verify>
    1. Review the three modified files to confirm the warm transfer configuration looks correct:
       - `src/app/api/call/start/route.ts` -- transferCall tool in model.tools
       - `src/app/api/webhooks/vapi/route.ts` -- simplified human_detected, warm transfer detection
       - `src/lib/stall-prompt.ts` -- transferCall directive

    2. Verify `npm run build` passes (Task 1 should have confirmed this)

    3. Optionally: Test with a live call to verify the warm transfer flow works end-to-end:
       - Start a call from the UI
       - AI should navigate IVR and detect a human agent
       - AI should introduce itself, then call transferCall
       - You should receive a call on your phone from the transfer assistant
       - After answering, the ISP agent and you should be connected
       - UI should show "Connected" status

    4. If you want to skip live testing for now, just verify the code looks correct and approve.
  </how-to-verify>
  <resume-signal>Type "approved" to complete the warm transfer pivot, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` passes
2. No dead references to Twilio Conference infrastructure
3. Warm transfer configuration verified in all modified files
4. Human has reviewed and approved the changes
</verification>

<success_criteria>
- Build is clean with no errors
- No Twilio Conference code remains in the codebase
- Warm transfer is fully configured and ready for live testing
- Human has verified the architectural pivot is correct
</success_criteria>

<output>
After completion, create `.planning/phases/04-transfer-bridging/04-05-SUMMARY.md`
</output>
