---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/lib/auth.ts
  - src/lib/auth-client.ts
  - src/lib/twilio.ts
  - src/app/api/auth/[...all]/route.ts
  - src/middleware.ts
  - src/app/page.tsx
  - src/app/verify/page.tsx
  - src/components/phone-input.tsx
  - src/components/otp-input.tsx
autonomous: true
user_setup:
  - service: twilio
    why: "SMS OTP verification via Twilio Verify"
    env_vars:
      - name: TWILIO_ACCOUNT_SID
        source: "Twilio Console -> Account Info -> Account SID"
      - name: TWILIO_AUTH_TOKEN
        source: "Twilio Console -> Account Info -> Auth Token"
      - name: TWILIO_VERIFY_SERVICE_SID
        source: "Twilio Console -> Develop -> Verify -> Services -> Create Service -> Service SID (starts with VA)"
    account_setup:
      - task: "Create Twilio account (free trial)"
        location: "https://www.twilio.com/try-twilio"
    dashboard_config:
      - task: "Create a Verify Service"
        location: "Twilio Console -> Develop -> Verify -> Services -> Create new"
  - service: better-auth
    why: "Auth secret for session signing"
    env_vars:
      - name: BETTER_AUTH_SECRET
        source: "Generate with: openssl rand -base64 32"

must_haves:
  truths:
    - "User can enter their phone number and receive an SMS OTP"
    - "User can enter the OTP code and get authenticated (new user auto-created, returning user logged in)"
    - "After verification, user is redirected to /select-isp"
    - "Authenticated session persists across browser restarts (30-day expiry)"
    - "Unauthenticated users visiting /select-isp, /select-issue, or /confirm are redirected to /"
    - "OTP errors show toast notifications (wrong code, expired code, delivery failure)"
  artifacts:
    - path: "src/lib/auth.ts"
      provides: "BetterAuth server config with phone number plugin + Twilio Verify"
      exports: ["auth"]
      contains: "phoneNumber"
    - path: "src/lib/auth-client.ts"
      provides: "BetterAuth client config with phone number client plugin"
      exports: ["authClient"]
      contains: "phoneNumberClient"
    - path: "src/lib/twilio.ts"
      provides: "Twilio Verify send and check helpers"
      exports: ["sendPhoneOTP", "verifyPhoneOTP"]
      contains: "verify.v2"
    - path: "src/app/api/auth/[...all]/route.ts"
      provides: "BetterAuth API handler"
      exports: ["POST", "GET"]
    - path: "src/middleware.ts"
      provides: "Route protection for authenticated pages"
      contains: "protectedRoutes"
    - path: "src/app/page.tsx"
      provides: "Phone number input page (step 1)"
      contains: "phoneNumber"
    - path: "src/app/verify/page.tsx"
      provides: "OTP verification page (step 2)"
      contains: "verify"
  key_links:
    - from: "src/lib/auth.ts"
      to: "src/lib/twilio.ts"
      via: "sendOTP/verifyOTP delegation"
      pattern: "sendPhoneOTP|verifyPhoneOTP"
    - from: "src/lib/auth.ts"
      to: "src/db/index.ts"
      via: "drizzleAdapter"
      pattern: "drizzleAdapter.*db"
    - from: "src/app/api/auth/[...all]/route.ts"
      to: "src/lib/auth.ts"
      via: "toNextJsHandler"
      pattern: "toNextJsHandler.*auth"
    - from: "src/app/page.tsx"
      to: "src/lib/auth-client.ts"
      via: "authClient.phoneNumber.sendOtp"
      pattern: "sendOtp"
    - from: "src/app/verify/page.tsx"
      to: "src/lib/auth-client.ts"
      via: "authClient.phoneNumber.verify"
      pattern: "\\.verify"
    - from: "src/middleware.ts"
      to: "better-auth/cookies"
      via: "getSessionCookie"
      pattern: "getSessionCookie"
---

<objective>
Implement the complete phone-based authentication flow: BetterAuth with Twilio Verify for SMS OTP, phone input page, OTP verification page, session persistence, and route protection middleware.

Purpose: Users must be able to authenticate before accessing ISP selection. This is the auth backbone of the entire app -- single phone input, auto-detect new vs returning, persistent sessions.
Output: Working auth flow where user enters phone number, receives SMS OTP, verifies it, and is redirected to /select-isp with a persistent session. Protected routes redirect unauthenticated users to /.
</objective>

<execution_context>
@/Users/brad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure BetterAuth server and client with Twilio Verify integration</name>
  <files>
    src/lib/auth.ts
    src/lib/auth-client.ts
    src/lib/twilio.ts
    src/app/api/auth/[...all]/route.ts
    src/middleware.ts
  </files>
  <action>
    1. Create `src/lib/twilio.ts`:
       - Use the EXACT code from RESEARCH.md Pattern 1 (Twilio Verify helpers)
       - `sendPhoneOTP(phoneNumber)`: Calls `client.verify.v2.services(SID).verifications.create({ to: phoneNumber, channel: "sms" })`
       - `verifyPhoneOTP(phoneNumber, code)`: Calls `client.verify.v2.services(SID).verificationChecks.create({ to, code })`, returns `check.status === "approved"`
       - Wrap verifyPhoneOTP in try/catch, return false on error

    2. Create `src/lib/auth.ts`:
       - Use the EXACT code from RESEARCH.md Pattern 1 (BetterAuth config)
       - Import `betterAuth` from `better-auth`
       - Import `drizzleAdapter` from `better-auth/adapters/drizzle`
       - Import `phoneNumber` plugin from `better-auth/plugins`
       - Import `nextCookies` from `better-auth/next-js`
       - Import `db` from `@/db`
       - Import `sendPhoneOTP`, `verifyPhoneOTP` from `@/lib/twilio`
       - Configure phone number plugin with:
         - `sendOTP`: Call `sendPhoneOTP(phoneNumber)` -- do NOT await (BetterAuth docs warn against it). IGNORE the `code` parameter (Twilio generates its own).
         - `verifyOTP`: `return await verifyPhoneOTP(phoneNumber, code)` -- must return boolean
         - `signUpOnVerification`: Auto-create users with temp email `{phoneNumber}@phone.easycallai.local` and temp name as phone number
       - `nextCookies()` MUST be the LAST plugin in the array
       - Session config: `expiresIn: 60 * 60 * 24 * 30` (30 days), `updateAge: 60 * 60 * 24` (daily refresh), `cookieCache: { enabled: true, maxAge: 60 * 60 * 24 * 7 }` (7-day cache)

    3. Create `src/lib/auth-client.ts`:
       - Use the EXACT code from RESEARCH.md Pattern 2
       - `createAuthClient` from `better-auth/react`
       - Add `phoneNumberClient()` plugin
       - Export `authClient`

    4. Create `src/app/api/auth/[...all]/route.ts`:
       - Use the EXACT code from RESEARCH.md (BetterAuth API Route)
       - `toNextJsHandler(auth)` -> export `{ POST, GET }`

    5. Create `src/middleware.ts`:
       - Use the EXACT code from RESEARCH.md (Middleware for Route Protection)
       - Protected routes: `/select-isp`, `/select-issue`, `/confirm`
       - If no session cookie, redirect to `/`
       - Matcher config for the three protected routes
  </action>
  <verify>
    - `npm run build` succeeds (or at least `npx tsc --noEmit` passes for the new files)
    - Files exist: src/lib/auth.ts, src/lib/auth-client.ts, src/lib/twilio.ts, src/app/api/auth/[...all]/route.ts, src/middleware.ts
    - auth.ts imports from twilio.ts (grep for sendPhoneOTP)
    - auth.ts has nextCookies() as last plugin
    - middleware.ts protects /select-isp, /select-issue, /confirm
  </verify>
  <done>
    BetterAuth configured with Twilio Verify delegation, phone number auto-detection (signUpOnVerification), 30-day persistent sessions, and route protection middleware. API route handler exports GET and POST.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build phone input and OTP verification pages</name>
  <files>
    src/app/page.tsx
    src/app/verify/page.tsx
    src/components/phone-input.tsx
    src/components/otp-input.tsx
  </files>
  <action>
    **IMPORTANT: Use the frontend-design skill for all UI work in this task (per user decision).**

    1. Create `src/components/phone-input.tsx`:
       - A controlled input component for US phone numbers
       - Use shadcn `Input` and `Label` components
       - Use react-hook-form with zod validation
       - Zod schema: validate E.164 format for US numbers (must start with +1, 10 digits after country code)
       - Accept input as `(555) 123-4567` or `5551234567` and normalize to `+15551234567` before submission
       - Simple input mask or formatting as user types (parentheses, dashes for US format)
       - Props: `onSubmit(phoneNumber: string)` callback with E.164 formatted number
       - Show validation error below input (zod error message)

    2. Create `src/app/page.tsx` (replace the default Next.js page):
       - This is Step 1 of the flow: phone number input
       - "use client" component (needs form interactivity)
       - Clean, centered layout: logo/title at top, phone input in the middle, "Continue" button below
       - Title: "EasyCallAI" with a subtitle like "Never wait on hold again"
       - On submit: call `authClient.phoneNumber.sendOtp({ phoneNumber })`
       - On success: redirect to `/verify?phone={encodeURIComponent(phoneNumber)}` (pass phone via URL params -- RESEARCH.md Pitfall 4)
       - On error: show toast via Sonner -- `toast.error("Failed to send verification code. Please try again.")`
       - Show loading state on button while sending
       - If user is already authenticated (check with `authClient.useSession()`), redirect to `/select-isp` immediately
       - Visual design: clean minimal (white background, generous whitespace) per discretion

    3. Create `src/components/otp-input.tsx`:
       - A 6-digit OTP code input component
       - Use shadcn `Input` component
       - Can be a single input field that accepts 6 digits, or 6 individual boxes (Claude's discretion -- recommend single input for simplicity)
       - Use react-hook-form with zod validation (exactly 6 digits)
       - Props: `onSubmit(code: string)` callback
       - Auto-focus on mount

    4. Create `src/app/verify/page.tsx`:
       - This is Step 2 of the flow: OTP verification
       - "use client" component
       - Read phone number from URL search params (`useSearchParams()`)
       - If no phone param, redirect back to `/`
       - Display: "Enter the code sent to {formatted phone number}"
       - OTP input component + "Verify" button
       - On submit: call `authClient.phoneNumber.verify({ phoneNumber, code })`
       - On success: redirect to `/select-isp` using `router.push("/select-isp")`
       - On error: show toast notifications per locked decision:
         - Wrong code: `toast.error("Invalid code. Please try again.")`
         - Expired: `toast.error("Code expired. Please request a new one.")`
         - General failure: `toast.error("Verification failed. Please try again.")`
       - "Resend code" link/button (calls sendOtp again, shows success toast)
       - Show loading state on verify button while verifying
       - Visual design: same clean minimal style, centered layout
  </action>
  <verify>
    - `npm run dev` starts and both pages render without errors
    - Visit `http://localhost:3000` -- see phone number input with "EasyCallAI" title
    - Visit `http://localhost:3000/verify` -- redirects to `/` (no phone param)
    - Visit `http://localhost:3000/verify?phone=%2B15551234567` -- see OTP input with phone number displayed
    - Visit `http://localhost:3000/select-isp` while unauthenticated -- redirects to `/`
    - No TypeScript errors: `npx tsc --noEmit`
  </verify>
  <done>
    Phone input page at `/` accepts US phone numbers and sends OTP via BetterAuth/Twilio. OTP page at `/verify` accepts 6-digit code and authenticates user. Errors display as Sonner toasts. Successful verification redirects to /select-isp. Already-authenticated users on `/` redirect to /select-isp.
  </done>
</task>

</tasks>

<verification>
1. Full auth flow works end-to-end (with real Twilio credentials):
   - Enter phone number on `/` -> receive SMS -> enter code on `/verify` -> redirected to `/select-isp`
2. New user: auto-created in database with phone number
3. Returning user: logs in without creating duplicate
4. Session persists after closing and reopening browser
5. Protected routes (/select-isp, /select-issue, /confirm) redirect to `/` when unauthenticated
6. OTP errors show appropriate toast messages
7. No TypeScript errors
</verification>

<success_criteria>
- Phone number input validates and formats to E.164
- OTP sent via Twilio Verify (not Programmable Messaging)
- Verification creates/finds user via BetterAuth signUpOnVerification
- Session cookie set with 30-day expiry
- Middleware blocks unauthenticated access to protected routes
- Toast notifications for all error states
- Clean, minimal UI using shadcn components
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
