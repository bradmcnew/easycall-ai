---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/app/select-isp/page.tsx
  - src/app/select-issue/page.tsx
  - src/app/confirm/page.tsx
  - src/components/step-indicator.tsx
  - src/components/isp-card.tsx
  - src/components/issue-button.tsx
  - src/app/layout.tsx
  - public/logos/comcast.svg
  - public/logos/att.svg
  - public/logos/spectrum.svg
  - public/logos/verizon.svg
  - public/logos/cox.svg
  - public/logos/centurylink.svg
autonomous: false

must_haves:
  truths:
    - "User can see 6 ISPs displayed as branded cards in a 2x3 grid"
    - "User can click an ISP card and navigate to issue category selection"
    - "User sees per-ISP issue categories (not a universal list) loaded from the database"
    - "User can select an issue category and optionally write a note"
    - "User sees a confirmation screen showing selected ISP, issue category, and note"
    - "Step indicator shows current progress across all steps"
    - "Back navigation works via browser back button and clickable step dots"
  artifacts:
    - path: "src/app/select-isp/page.tsx"
      provides: "ISP selection grid page (step 3)"
      contains: "isp"
    - path: "src/app/select-issue/page.tsx"
      provides: "Issue category selection page (step 4)"
      contains: "category"
    - path: "src/app/confirm/page.tsx"
      provides: "Confirmation/review page (step 5)"
      contains: "confirm"
    - path: "src/components/step-indicator.tsx"
      provides: "Progress dots showing current step"
      contains: "step"
    - path: "src/components/isp-card.tsx"
      provides: "ISP card component with logo and name"
      contains: "logo"
    - path: "src/components/issue-button.tsx"
      provides: "Issue category button with label and description"
      contains: "category"
  key_links:
    - from: "src/app/select-isp/page.tsx"
      to: "database"
      via: "server component query for ISPs"
      pattern: "db\\.query|db\\.select"
    - from: "src/app/select-issue/page.tsx"
      to: "database"
      via: "server component query for categories by ISP"
      pattern: "issueCategory|isp_id"
    - from: "src/app/select-isp/page.tsx"
      to: "/select-issue"
      via: "navigation on card click"
      pattern: "select-issue.*isp"
    - from: "src/app/select-issue/page.tsx"
      to: "/confirm"
      via: "navigation on category select"
      pattern: "confirm"
    - from: "src/components/step-indicator.tsx"
      to: "usePathname"
      via: "current route detection"
      pattern: "usePathname"
---

<objective>
Build the ISP selection, issue category, and confirmation pages with step indicator navigation. Users browse ISPs in a branded card grid, select per-ISP issue categories, optionally add a note, and review their selections.

Purpose: This completes the call configuration flow -- after auth, users set up everything needed to place a call (which happens in Phase 2). The confirmation page is the natural endpoint of Phase 1.
Output: Three connected pages (/select-isp, /select-issue, /confirm) with step indicator, ISP logos, per-ISP categories from database, and a review/confirm screen.
</objective>

<execution_context>
@/Users/brad/.claude/get-shit-done/workflows/execute-plan.md
@/Users/brad/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ISP selection page with branded card grid and step indicator</name>
  <files>
    src/components/step-indicator.tsx
    src/components/isp-card.tsx
    src/app/select-isp/page.tsx
    src/app/layout.tsx
    public/logos/comcast.svg
    public/logos/att.svg
    public/logos/spectrum.svg
    public/logos/verizon.svg
    public/logos/cox.svg
    public/logos/centurylink.svg
  </files>
  <action>
    **IMPORTANT: Use the frontend-design skill for all UI work in this task (per user decision).**

    1. Create ISP logo SVG placeholder files in `public/logos/`:
       - Create simple, recognizable placeholder SVGs for each ISP
       - Each should be a clean SVG with the ISP name in a styled text element, roughly 200x100px
       - Files: comcast.svg, att.svg, spectrum.svg, verizon.svg, cox.svg, centurylink.svg
       - Use brand-appropriate colors where possible (Comcast red, AT&T blue, Spectrum blue, Verizon red, Cox blue, CenturyLink green)
       - These are placeholders -- real logos can be swapped in later

    2. Create `src/components/step-indicator.tsx`:
       - "use client" component (needs usePathname)
       - Shows 5 step dots in a horizontal row: Phone, Verify, Select ISP, Select Issue, Confirm
       - Uses `usePathname()` to determine current step
       - Step mapping: `/` = 1, `/verify` = 2, `/select-isp` = 3, `/select-issue` = 4, `/confirm` = 5
       - Visual states: completed (filled), current (filled + larger/ring), upcoming (outline/gray)
       - Steps 3-5 are clickable links when user is on a step >= that step (e.g., on step 4, steps 3-4 are clickable, step 5 is not)
       - Steps 1-2 (auth steps) are NOT clickable from steps 3+ (can't go back to re-auth)
       - Small label below each dot (visible on md+ screens, hidden on mobile for space)
       - Clean minimal style -- subtle gray dots, primary color for active

    3. Update `src/app/layout.tsx`:
       - Add the StepIndicator component to the layout, positioned above {children}
       - Wrap content in a centered container with max-width (e.g., `max-w-2xl mx-auto px-4 py-8`)
       - Keep the Toaster component

    4. Create `src/components/isp-card.tsx`:
       - Uses shadcn `Card` component
       - Displays ISP logo (from /logos/{slug}.svg) and ISP name
       - Name and logo ONLY -- no subtitles, hold times, or extra metadata (locked decision)
       - Hover state: subtle border color change or shadow
       - Focus state for keyboard accessibility
       - Props: `isp: { id: string; slug: string; name: string; logoUrl: string }`, `onClick` or wraps in a Link
       - Responsive: cards should look good at various sizes

    5. Create `src/app/select-isp/page.tsx`:
       - This is Step 3 of the flow
       - Server component that fetches ISPs from the database: `db.query.isp.findMany()` (or `db.select().from(isp)`)
       - Renders a 2x3 responsive grid of ISP cards (2 columns on mobile, 3 on larger screens -- or 2x3 as 3 cols x 2 rows on desktop, stacking on mobile)
       - Grid layout: `grid grid-cols-2 lg:grid-cols-3 gap-4`
       - Each card is a Link to `/select-issue?isp={isp.slug}`
       - Page title: "Select Your ISP" or "Who's your internet provider?"
       - No search bar (locked decision -- only 6 items)
       - No "my ISP isn't listed" fallback (locked decision)
       - Browse only -- no filtering needed
  </action>
  <verify>
    - `npm run dev` and visit `http://localhost:3000/select-isp` (may need to manually navigate or temporarily remove auth middleware for testing)
    - 6 ISP cards render in a grid layout
    - Each card shows an ISP logo and name
    - Clicking a card navigates to `/select-issue?isp={slug}`
    - Step indicator shows step 3 as active
    - Layout is centered with appropriate max-width
    - No TypeScript errors: `npx tsc --noEmit`
  </verify>
  <done>
    ISP selection page displays 6 ISPs in a 2x3 branded card grid. Step indicator shows progress. Cards link to issue selection with ISP slug in URL params. Clean minimal design with shadcn Card components. Logo SVG placeholders in public/logos/.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create issue category selection and confirmation pages</name>
  <files>
    src/components/issue-button.tsx
    src/app/select-issue/page.tsx
    src/app/confirm/page.tsx
  </files>
  <action>
    **IMPORTANT: Use the frontend-design skill for all UI work in this task (per user decision).**

    1. Create `src/components/issue-button.tsx`:
       - Uses shadcn `Button` with `variant="outline"` and custom layout
       - Full-width, stacked vertically
       - Shows category label (bold) and description (smaller, muted text) on two lines
       - Hover state: subtle background change
       - Props: `category: { id: string; slug: string; label: string; description: string | null }`, `onClick` or wraps in Link
       - Accessible: proper button role, focus states

    2. Create `src/app/select-issue/page.tsx`:
       - This is Step 4 of the flow
       - Server component that reads `?isp={slug}` from search params
       - If no ISP param or invalid ISP, redirect to `/select-isp`
       - Fetches the ISP and its categories from the database:
         - Query ISP by slug: `db.query.isp.findFirst({ where: eq(isp.slug, ispSlug) })`
         - Query categories: `db.select().from(issueCategory).where(eq(issueCategory.ispId, ispRecord.id)).orderBy(issueCategory.sortOrder)`
       - Page title: "What do you need help with?" with ISP name shown (e.g., "Comcast - What do you need help with?")
       - Display per-ISP categories as full-width stacked buttons (locked decision: per-ISP categories)
       - Below categories: an optional free-text note field (locked decision)
         - Use shadcn `Input` or a `Textarea` component
         - Label: "Add a note (optional)" or "Describe your issue briefly (optional)"
         - Placeholder: "e.g., My internet has been dropping every hour for the past week"
       - Selecting a category + clicking "Continue" navigates to `/confirm?isp={slug}&category={category.slug}&note={encodeURIComponent(note)}`
       - The category selection should be a click-to-select pattern: click a category button to highlight it, then click Continue. Or: clicking the category itself navigates (Claude's discretion -- recommend click-to-select + Continue button for consistency with the note field).

    3. Create `src/app/confirm/page.tsx`:
       - This is Step 5 of the flow
       - Server component that reads `?isp={slug}&category={slug}&note={text}` from search params
       - If missing ISP or category params, redirect to `/select-isp`
       - Fetches ISP and category from database by slugs
       - Display a review card showing:
         - ISP: logo + name (with "Change" link back to `/select-isp`)
         - Issue: category label + description (with "Change" link back to `/select-issue?isp={slug}`)
         - Note: user's note text if provided (with "Edit" link back to `/select-issue?isp={slug}`)
       - Primary action button: "Start Call" (DISABLED in Phase 1 -- call placement is Phase 2)
         - Show the button but disabled with tooltip or helper text: "Coming soon" or similar
         - This provides a natural stopping point and shows the Phase 2 integration point
       - Alternative: Instead of a disabled button, show a success state: "All set! Call placement coming soon." This avoids the awkwardness of a disabled button.
       - Claude's discretion on exact wording -- key is that the page confirms the user's selections and is the natural end of Phase 1
       - Use shadcn Card for the review layout
  </action>
  <verify>
    - Visit `/select-issue?isp=comcast` -- see Comcast-specific categories
    - Visit `/select-issue?isp=att` -- see AT&T-specific categories (may differ from Comcast)
    - Visit `/select-issue` (no param) -- redirects to `/select-isp`
    - Select a category, optionally type a note, click Continue -- navigates to `/confirm` with params
    - Visit `/confirm?isp=comcast&category=billing` -- see review card with Comcast + Billing
    - Step indicator shows correct step (4 for select-issue, 5 for confirm)
    - "Change" links navigate back to correct pages
    - No TypeScript errors: `npx tsc --noEmit`
  </verify>
  <done>
    Issue category page shows per-ISP categories loaded from database with optional note field. Confirmation page shows review of all selections (ISP, category, note) with change/edit links. Step indicator reflects progress. Phase 1 flow complete: phone -> OTP -> ISP -> issue -> confirm.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete Phase 1 flow</name>
  <what-built>
    Complete Phase 1 user flow: phone number auth via SMS OTP, ISP selection from branded card grid, per-ISP issue category selection with optional note, and confirmation/review page. Step indicator shows progress throughout.
  </what-built>
  <how-to-verify>
    1. Start dev server: `npm run dev`
    2. Visit http://localhost:3000
       - Should see "EasyCallAI" title and phone number input
       - Step indicator shows step 1 active
    3. Enter your phone number and click Continue
       - Should receive SMS OTP (requires real Twilio credentials in .env.local)
       - Should redirect to /verify with your phone number displayed
    4. Enter the OTP code
       - Should authenticate and redirect to /select-isp
       - Test wrong code first -- should see error toast
    5. On /select-isp:
       - Should see 6 ISP cards in a grid with logos
       - Step indicator shows step 3
       - Click an ISP (e.g., Comcast)
    6. On /select-issue:
       - Should see categories specific to the ISP you chose
       - Select a category, optionally type a note
       - Click Continue
    7. On /confirm:
       - Should see your ISP, category, and note
       - "Change" links should go back to correct pages
       - Step indicator shows step 5
    8. Test back navigation: use browser back button through the flow
    9. Close browser completely, reopen, visit http://localhost:3000
       - Should still be logged in (persistent session)
       - Should redirect to /select-isp (or wherever appropriate)
    10. Visit /select-isp in an incognito window -- should redirect to /
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. ISP grid shows 6 branded cards (2x3 layout)
2. Clicking ISP navigates to per-ISP categories
3. Categories are loaded from database, not hardcoded in the component
4. Optional note field accepts free text
5. Confirmation page shows all selections with change links
6. Step indicator is accurate across all 5 pages
7. Full flow works: / -> /verify -> /select-isp -> /select-issue -> /confirm
8. Protected routes redirect unauthenticated users
</verification>

<success_criteria>
- 6 ISPs render in a branded card grid with logos
- Issue categories are per-ISP (loaded from database based on ISP selection)
- Optional note field present on issue selection page
- Confirmation page reviews all selections with edit capability
- Step indicator spans all pages and reflects current step
- All pages use shadcn components with clean minimal design
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
